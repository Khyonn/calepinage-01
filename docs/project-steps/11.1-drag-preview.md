# Sous-étape 11.1 — Drag segment + preview temps réel

## 1. Objectif utilisateur

En mode `edit` sur une pièce active, l'utilisateur appuie sur le bouton gauche au-dessus d'un segment, glisse horizontalement, et voit les lames de ce segment se repositionner en temps réel. Le curseur passe en `grabbing`. Au relâchement, la nouvelle valeur de `xOffset` est persistée.

## 2. Dépendances

- 10.2 ✅ (segments rendus et cliquables).
- 10.4 ✅ (visualisation des violations — nécessaire pour le feedback pendant drag).
- Domaine : `fillRow`, `validateRow` — déjà en place.

## 3. Fichiers à créer / modifier

**Créer :**
- `src/components/canvas/Row/Segment/useSegmentDrag.ts` — hook local : état `isDragging`, `previewOffset`, `lastValidOffset`, handlers pointer.
- `src/store/dragSlice.ts` (ou extension `uiSlice.ts` — trancher) — état de drag global si besoin d'être lu par d'autres composants (annotations, violations). Par défaut : local au composant, dispatch uniquement au release.

**Modifier :**
- `src/components/canvas/Row/Segment/index.tsx` — câbler `onPointerDown` → démarrer drag, `pointermove` / `pointerup` sur `window`. Curseur `grab` (hover) / `grabbing` (drag). Pendant drag, substituer les lames calculées à partir de `previewOffset` au lieu du store.
- `src/store/projectSlice.ts` — action `moveSegment({ roomId, rowId, segmentIndex, xOffset })` (si absente) dispatchée au release.
- `tests/features/move-row.feature` — renommer ou adapter vers `move-segment.feature` si le nom historique est incorrect (le domaine opère sur `segments[].xOffset`).

## 4. Comportement attendu

- [ ] Hover sur un segment : curseur `grab`.
- [ ] PointerDown bouton gauche : démarrer drag, curseur `grabbing`, opacité du segment 70 %, contour `--accent`.
- [ ] PointerMove : calculer `previewOffset = initialOffset - (deltaScreenX / zoom)`, quantifier à 0,1 cm.
- [ ] Preview recalcule `fillRow(previewOffset, ...)` en temps réel, sans dispatch.
- [ ] Si `validateRow(previewOffset, ...)` renvoie une violation bloquante (ex. `first-plank-too-short` au point extrême), ne PAS mettre à jour `lastValidOffset` — preview visuel peut continuer à avancer mais la valeur committée reste la dernière valide. **À confirmer** : alternative = preview bloqué au dernier valide. Trancher et documenter le choix dans le commit.
- [ ] PointerUp : dispatch `moveSegment({ xOffset: lastValidOffset })`, retour à l'opacité normale, curseur `grab`.
- [ ] Échap pendant drag : annuler (aucun dispatch), revenir à l'état initial.
- [ ] Annotations : **décision à prendre ici** — suivent le preview OU figées pendant drag. Documenter dans le fichier et dans `docs/features/constraints-annotations.md` le cas échéant.

## 5. Tests

- **BDD** existant (`tests/features/move-row.feature` ou équivalent) : couvre la logique de validation. Vérifier que ça porte bien sur `segments[].xOffset` après refonte de l'étape 05. Ajouter un scénario si nécessaire.
- **Manuel** : tester sur une rangée valide, une rangée en limite de contrainte, un drag qui traverse une zone invalide.

## 6. Références doc

- [features/row-drag.md](../features/row-drag.md)
- [features/constraints-annotations.md](../features/constraints-annotations.md)
- [technical/architecture.md](../technical/architecture.md) — pattern preview local vs dispatch.

## 7. Critères d'acceptation

- [ ] Drag fluide (pas de re-render global par frame).
- [ ] Preview uniquement local — aucun dispatch par pointermove.
- [ ] Dispatch unique au release.
- [ ] Échap annule proprement.
- [ ] `tsc --noEmit` passe, `bun run test` reste vert.
- [ ] Taille du hook ≤ 150 lignes.
- [ ] Décision sur annotations pendant drag documentée (dans ce fichier + commit message).
