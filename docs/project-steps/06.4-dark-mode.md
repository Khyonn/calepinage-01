# Sous-étape 06.4 — Dark mode

## 1. Objectif utilisateur

L'utilisateur peut choisir entre les thèmes light, dark et system via le menu hamburger. Le choix est persisté et restauré au prochain chargement. Tous les écrans respectent le thème actif.

## 2. Dépendances

- 06.1 (topbar + hamburger), 06.2 (menu hamburger peuplé).

## 3. Fichiers à créer / modifier

**Créer :**
- `src/components/layout/ThemeSwitcher/index.tsx` — élément de menu avec RadioGroup (light / dark / system).
- `src/components/layout/ThemeSwitcher/useThemeSwitcher.ts` — logique d'application (attribut `data-theme` sur `<html>`, écoute de `prefers-color-scheme` en mode system).
- `src/hooks/useTheme.ts` — hook partagé exposant `{ theme, setTheme }`.

**Modifier :**
- `src/components/layout/HamburgerMenu/index.tsx` — ajouter l'entrée "Thème" → `ThemeSwitcher` en sous-menu flyout.
- `src/persistence/db.ts` — object store `meta` (créé en 06.2) : ajouter un champ `theme: 'light' | 'dark' | 'system'`.
- `src/main.tsx` — au boot, lire le thème depuis `meta` (ou `prefers-color-scheme` par défaut) et appliquer l'attribut `data-theme` **avant** le premier render pour éviter un flash.
- `src/index.css` — confirmer que les tokens `--bg`, `--surface`, `--text`, etc. ont bien un overload `[data-theme="dark"]` (déjà en place depuis étape 04 — vérifier).

## 4. Comportement attendu

- [ ] Au premier lancement, thème = `system`. L'attribut `data-theme` reflète `prefers-color-scheme` (light ou dark).
- [ ] Choix `light` → attribut `data-theme="light"`, persisté.
- [ ] Choix `dark` → attribut `data-theme="dark"`, persisté.
- [ ] Choix `system` → écoute `matchMedia('(prefers-color-scheme: dark)')` et met à jour l'attribut dynamiquement.
- [ ] Au reload, le thème choisi est restauré avant le premier render (pas de flash de thème opposé).
- [ ] Tous les panneaux, dialogs, et le placeholder canvas respectent le thème actif (vérification visuelle).

## 5. Tests

- **BDD non critique** à ce stade — la logique est surtout visuelle.
- Possible unit test léger : `useThemeSwitcher` avec jsdom (mock `matchMedia`) si ROI justifié. Sinon, tests manuels.

## 6. Références doc

- [features/ui-specifications.md](../features/ui-specifications.md) — dark mode section.
- [technical/code-conventions.md](../technical/code-conventions.md) — utilisation de `data-theme`.

## 7. Critères d'acceptation

- [ ] Les 3 modes (light / dark / system) fonctionnent et sont persistés.
- [ ] Pas de flash au chargement (thème appliqué avant hydratation React).
- [ ] `tsc --noEmit` passe.
- [ ] `bun run test` reste vert.
- [ ] Les tokens utilisés dans tout nouveau CSS respectent le contraste AA sur les deux thèmes (vérif visuelle).
